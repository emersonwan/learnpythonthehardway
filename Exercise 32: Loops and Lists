# -*- coding: utf-8 -*-
# Rodar como: python lpthw.py
print "\nExercício 32" # Exercício 32
the_count = [1, 2, 3, 4, 5] # Cria uma lista com 5 itens
fruits = ['apples', 'oranges', 'pears', 'apricots']
change = [1, 'pennies', 2, 'dimes', 3, 'quarters']

# this first kind of for-loop goes through a list
for number in the_count: # number é uma variável genérica usada só no for-loop
# number recebe o item em the_count e incrementa o passo automaticamente
	print "This is count %d" % number

# same as above
for fruit in fruits: # fruit é a variável genérica usada só no for-loop
# fruit recebe o item em fruits e incrementa o passo automaticamente
	print "A fruit of type: %s" % fruit

# also we can go through mixed lists too
# notice we have to use %r since we don't know what's in it
for i in change:
	print "I got %r" % i

# we can also build lists, first start with an empty one
elements = [] # Cria uma lista vazia

# then use the range function to do 0 to 5 counts
for i in range(0, 6): # range vai de 0 a 5; é até 6, logo 5 não entra
	print "Adding %d to the list." % i
	# append is a function that lists understand
	elements.append(i) # Acrescenta i na lista elements

# now we can print them out too
for i in elements: # Imprime a lista elements
	print "Element was: %d" % i
